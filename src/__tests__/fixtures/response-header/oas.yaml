openapi: 3.0.0
info:
  title: OAS
  version: 1.0.0
paths:
  /path:
    get:
      responses:
        "200":
          description: Success
          headers:
            specified:
              schema:
                type: number
          content:
            application/json:
              schema: {}
