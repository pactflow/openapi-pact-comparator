openapi: 3.1.0
info:
  title: Image API
  version: 1.0.0
paths:
  /image:
    get:
      responses:
        "200":
          description: OK
          content:
            image/png:
              schema:
                type: string
                format: binary
  /image-optional:
    get:
      responses:
        "200":
          description: OK
          content:
            image/png:
              schema:
                type: string
                format: binary
