openapi: 3.0.0
info:
  title: OAS
  version: 1.0.0

paths:
  /exists:
    get:
      responses:
        "200":
          description: description
          content:
            "application/json":
              schema: {}
