swagger: "2.0"
info:
  title: OAS
  version: 1.0.0
paths:
  /path:
    get:
      produces:
        - application/json
      responses:
        "200":
          description: OK
          headers:
            specified:
              type: number
          schema: {}
