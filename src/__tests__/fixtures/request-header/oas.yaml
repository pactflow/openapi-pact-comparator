openapi: 3.0.0
info:
  title: OAS
  version: 1.0.0
paths:
  /path:
    get:
      parameters:
        - in: header
          name: x-custom
          required: true
          schema:
            type: number
      responses:
        "200":
          description: Success
          content:
            application/json: {}
