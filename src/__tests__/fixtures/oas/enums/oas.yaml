openapi: 3.0.0
info:
  title: OAS
  version: 1.0.0

paths:
  /foo:
    get:
      responses:
        "200":
          description: OK
          content:
            "application/json":
              schema:
                type: object
                properties:
                  foo:
                    type: string
                    enum: ["A", "B", "C"]
