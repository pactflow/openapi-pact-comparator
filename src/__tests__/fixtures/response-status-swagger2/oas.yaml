swagger: "2.0"
info:
  title: multiple response
  version: 1.0.0
basePath: "/api"
paths:
  /animal:
    get:
      produces:
        - application/json
      responses:
        "200":
          description: OK
        "404":
          description: Not Found
  /plant:
    get:
      produces:
        - application/json
      responses:
        default:
          description: default
