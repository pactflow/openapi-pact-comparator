openapi: 3.0.0
info:
  title: V3 Message Events API
  version: 1.0.0
  description: |
    This OpenAPI spec is a placeholder for message-based (non-HTTP) interactions.
    The pact file contains v3 message interactions that cannot be directly validated
    against HTTP-based OpenAPI specifications. This fixture tests how the comparator
    handles v3 message pacts.

paths:
  /placeholder:
    get:
      summary: Placeholder endpoint
      description: This is not used - the pact contains message interactions only
      responses:
        "200":
          description: Placeholder response
